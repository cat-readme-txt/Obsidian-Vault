1. Existential:
	$\phi(t) \text{ for a term t} \models \exists x(\phi(x))$
2. Universal Elimination:
	$\forall (\phi(x)) \models \phi(t)$ for any term t
3. versal Introduction:
	$\phi(t) \text{ for an arbitrary term } t\models \forall x(\phi(x))$
4. Modus Poneus
	$\phi, \phi \implies \psi \models \psi$
5. 